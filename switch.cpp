#include <iostream>

using namespace std;

int main ()
{
    int a,b;
    char num;

    cout << "Enter the operator : ";
    cin >> num;
    cout <<"Enter the first number : ";
    cin >> a;
    cout << "Enter the secod numbeer :";
    cin >> b;

    switch(num)
}